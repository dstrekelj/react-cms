import { TableLayout as Component } from "./index";

const Story = (props) => <Component {...props} />;

export const TableLayoutStory = Story.bind({});

export default {
  title: "TableLayout",
  component: Component,
};
